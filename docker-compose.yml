services:
  echo-graalvm:
    image: ghcr.io/macielti/echo-graalvm:main
    container_name: echo-graalvm
    restart: always
    volumes:
      - ./resources/config.edn:/app/resources/config.edn
    deploy:
      resources:
        limits:
          memory: "25MB"
